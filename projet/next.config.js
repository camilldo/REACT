/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // swcMinify: true,
  reactStrictMode: false,
  distDir: "./build",
  outDir: "./out"
}

module.exports = nextConfig
